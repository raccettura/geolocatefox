/*************************************************
 * We define all our constants here, so we don't
 * have to do it in multiple places (usually).
 *************************************************/

const GEOLOCATEFOX_PREF_BRANCH     = "extensions.geolocatefox.";

const DEFAULT_MAP_SERVICE          = "yahoo";
const DEFAULT_Y_MAP_CLICK_MAG      = "6";
const DEFAULT_Y_MAP_TOOLTIP_MAG    = "15";

const DEFAULT_USE_HOSTIP           = false;

const DEFAULT_PRE_LOAD             = false;

/* XXX This app id also has to be entered in map.html */
const YAHOO_APP_ID                 = "geolocatefox";

const DEFAULT_TOOLTIP_MAP_WIDTH    = "225";
const DEFAULT_TOOLTIP_MAP_HEIGHT   = "150";
